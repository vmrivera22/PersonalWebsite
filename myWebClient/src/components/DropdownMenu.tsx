/*import LinkItem from "./LinkItem";
import { Link } from "react-router-dom";
import "../css/DropdownMenu.css";

interface MenuProps {
  links: Element[];
}

const DropdownMenu = ({ links }: MenuProps) => {
  return <div className="dropdown">{links}</div>;
};

export default DropdownMenu;*/
